/*
 * @lc app=leetcode.cn id=1314 lang=java
 * 
 * @lcpr version=21906
 *
 * [1314] 矩阵区域和
 */

// @lc code=start
class Solution {
    public int[][] matrixBlockSum(int[][] mat, int k) {
        // 暴力
        // int row = mat.length;
        // int col = mat[0].length;
        // int[][] ans = new int[row][col];
        // for (int i = 0; i < row; i++) {
        // for (int j = 0; j < col; j++) {
        // for (int r = i - k; r <= i + k; ++r)
        // for (int c = j - k; c <= j + k; ++c) {
        // // 坐标合法性
        // if (r >= 0 && c >= 0 && r < row & c < col)
        // ans[i][j] += mat[r][c];
        // }
        // }
        // }
        // return ans;

        // 前缀和

    }
}
// @lc code=end



/*
 * // @lcpr case=start // [[1,2,3],[4,5,6],[7,8,9]]\n1\n // @lcpr case=end
 * 
 * // @lcpr case=start // [[1,2,3],[4,5,6],[7,8,9]]\n2\n // @lcpr case=end
 * 
 */

